#!/bin/bash

docker run \
  --rm \
  -d \
  --network rebase \
  -w /app \
  -v $(pwd):/app \
  -v tmp:/tmp \
  -v rubygems:/usr/local/bundle/ \
  -p 3000:3000 \
  --name rubinho \
  ruby \
  bash -c "ruby server.rb"

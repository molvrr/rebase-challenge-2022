#!/bin/bash

docker run \
  --name sidekiq \
  --network rebase \
  --rm \
  -w /app \
  -v tmp:/tmp \
  -v rubygems:/usr/local/bundle \
  -v $(pwd):/app \
  ruby \
  bash -c "sidekiq -r ./server.rb"
